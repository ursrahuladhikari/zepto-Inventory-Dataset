# Zepto Inventory Data Analysis Using PostgreSQL

## Project Overview
This project focuses on analyzing the **Zepto Inventory Dataset** using **PostgreSQL**.

The analysis includes:
- PostgreSQL installation and setup
- Database and table creation
- CSV dataset import
- Data cleaning and transformation
- Exploratory Data Analysis (EDA)
- Business insights using SQL queries

---

# Tech Stack

- PostgreSQL
- SQL
- Excel (for UTF-8 CSV conversion)
- Kaggle Dataset

---

# Dataset Source

Dataset from Kaggle:

https://www.kaggle.com/datasets/palvinder2006/zepto-inventory-dataset/data?select=zepto_v2.csv

---

# PostgreSQL Installation

1. Visit the official PostgreSQL website:

https://www.postgresql.org/

2. Download the required version for:
   - Windows
   - macOS

3. Install PostgreSQL by following the setup instructions.

---

# PostgreSQL Database Setup

## Step 1: Create a Server
- Open PostgreSQL
- Create a new server
- Give the server a project-specific name

## Step 2: Create a Database
- Inside the server, create a new database for the project

## Step 3: Enter Password
- Use the same password created during PostgreSQL installation

## Step 4: Open Query Tool
- Right-click the database
- Select **Query Tool** for executing SQL queries

---

# Dataset Formatting

## Step 1: Extract Dataset
- Download the dataset ZIP file
- Extract it to a folder
- Rename the folder if required

Example:
```bash
Zepto Inventory
```

## Step 2: Convert CSV to UTF-8 Format
- Open the dataset in Excel
- Save the file as:

```bash
CSV UTF-8
```

> PostgreSQL supports UTF-8 encoded CSV files, helping avoid import errors.

---

# Create Table in PostgreSQL

## Delete Existing Table
```sql
DROP TABLE IF EXISTS zepto;
```

## Create New Table
```sql
CREATE TABLE zepto(
    sku_id SERIAL PRIMARY KEY,
    category VARCHAR(120),
    name VARCHAR(150) NOT NULL,
    mrp NUMERIC(8,2),
    discountPercent NUMERIC(5,2),
    availableQuantity INTEGER,
    discountedSellingPrice NUMERIC(8,2),
    WeightInGms INTEGER,
    OutOfStock BOOLEAN,
    quantity INTEGER
);
```

---

# Import CSV Dataset into PostgreSQL

1. Right-click the `zepto` table
2. Select:

```bash
Import/Export Data
```

3. Choose the converted UTF-8 CSV file

## Import Configuration

- Enable:

```bash
HEADER
```

- Delimiter:

```bash
,
```

- Remove `sku_id` under:
```bash
Columns to Import
```

4. Click **OK**

If successful, PostgreSQL will show:

```bash
Process Started
Process Completed
```

---

# Data Verification

## Check Imported Rows
```sql
SELECT COUNT(*) FROM zepto;
```

## View Sample Data
```sql
SELECT * FROM zepto
LIMIT 5;
```

---

# Checking NULL Values

```sql
SELECT * FROM zepto
WHERE 
    name IS NULL
    OR category IS NULL
    OR mrp IS NULL
    OR discountPercent IS NULL
    OR availableQuantity IS NULL
    OR discountedSellingPrice IS NULL
    OR WeightInGms IS NULL
    OR OutOfStock IS NULL
    OR quantity IS NULL;
```

---

# Exploratory Data Analysis (EDA)

## Different Product Categories
```sql
SELECT DISTINCT category
FROM zepto
ORDER BY category;
```

---

## Products In Stock vs Out Of Stock
```sql
SELECT OutOfStock, COUNT(sku_id)
FROM zepto
GROUP BY OutOfStock;
```

---

## Products Listed Multiple Times
```sql
SELECT name, COUNT(sku_id) AS "Number of SKUs"
FROM zepto
GROUP BY name
HAVING COUNT(sku_id) > 1
ORDER BY COUNT(sku_id) DESC;
```

---

# Data Cleaning

## Identify Products with Zero Pricing
```sql
SELECT * FROM zepto
WHERE mrp = 0
   OR discountedSellingPrice = 0;
```

## Delete Products with MRP = 0
```sql
DELETE FROM zepto
WHERE mrp = 0;
```

---

# Data Transformation

The pricing values are stored in **Paisa**.

Since:

```bash
100 Paisa = ₹1
```

Convert Paisa into Rupees.

## Convert Paisa to Rupees
```sql
UPDATE zepto
SET 
    mrp = mrp / 100.0,
    discountedSellingPrice = discountedSellingPrice / 100.0;
```

## Verify Updated Prices
```sql
SELECT mrp, discountedSellingPrice
FROM zepto;
```

---

# Business Insights

## 1. Top 10 Best-Value Products Based on Discount Percentage
```sql
SELECT DISTINCT name, mrp, discountPercent
FROM zepto
ORDER BY discountPercent DESC
LIMIT 10;
```

---

## 2. High MRP Products That Are Out Of Stock
```sql
SELECT DISTINCT name, mrp
FROM zepto
WHERE mrp > 300 
  AND OutOfStock = TRUE
ORDER BY mrp DESC
LIMIT 10;
```

---

## 3. Revenue Generated by Each Category
```sql
SELECT category,
       SUM(discountedSellingPrice * availableQuantity) AS total_revenue
FROM zepto
GROUP BY category
ORDER BY total_revenue;
```

---

## 4. Products with MRP Greater Than ₹500 and Discount Less Than 10%
```sql
SELECT DISTINCT name, mrp, discountPercent
FROM zepto
WHERE mrp > 500
  AND discountPercent < 10
ORDER BY mrp DESC, discountPercent DESC;
```

---

## 5. Top 5 Categories with Highest Average Discount
```sql
SELECT category,
       ROUND(AVG(discountPercent), 2) AS avg_discount
FROM zepto
GROUP BY category
ORDER BY avg_discount DESC
LIMIT 5;
```

---

## 6. Best Value Products Based on Price Per Gram
```sql
SELECT DISTINCT name,
       WeightInGms,
       discountedSellingPrice,
       ROUND(discountedSellingPrice / WeightInGms, 2) AS price_per_gram
FROM zepto
WHERE WeightInGms >= 100
ORDER BY price_per_gram;
```

---

## 7. Categorize Products Based on Weight
```sql
SELECT DISTINCT name,
       WeightInGms,
       CASE
           WHEN WeightInGms < 1000 THEN 'Low'
           WHEN WeightInGms < 5000 THEN 'Medium'
           ELSE 'Bulk'
       END AS weight_category
FROM zepto;
```

---

## 8. Total Inventory Weight Per Category
```sql
SELECT category,
       SUM(WeightInGms * availableQuantity) AS total_weight
FROM zepto
GROUP BY category
ORDER BY total_weight;
```

---

# Key Learnings

- Database creation and management using PostgreSQL
- Importing CSV datasets into PostgreSQL
- Data cleaning and transformation using SQL
- Performing EDA using SQL queries
- Generating business insights from inventory data

---

# Conclusion

This project demonstrates how PostgreSQL can be used for real-world inventory analysis and business intelligence.

The analysis provides insights into pricing, discounts, stock availability, product categories, and revenue generation.

---

# Author

Rahul Adhikari
